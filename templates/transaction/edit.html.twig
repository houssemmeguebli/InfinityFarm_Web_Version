{% extends 'base.html.twig' %}

{% block contenu %}
    <div class="container-fluid page-header">
        <div class="container">
            <div class="d-flex flex-column align-items-center justify-content-center" style="min-height: 400px">
                <h3 class="display-4 text-white text-uppercase">Modifier la transaction</h3>
                <div class="d-inline-flex text-white">
                    <p class="m-0 text-uppercase"><a class="text-white" href="">Home</a></p>
                    <i class="fa fa-angle-double-right pt-1 px-3"></i>
                    <p class="m-0 text-uppercase">Modifier la transaction</p>
                </div>
            </div>
        </div>
    </div>
<br>
<br>
    <h1>Modifier la transaction</h1>
<br>
<br>
    {{ include('transaction/_form.html.twig', {'button_label': 'Enregistrer'}) }}
    <button style="background-color: #8FB31D; color: white; border: none; border-radius: 5px; padding: 8px 12px; margin-right: 5px;" onclick="window.location.href='{{ path('app_transaction_index') }}'">Retour</button>

    {{ include('transaction/_delete_form.html.twig') }}
{% endblock %}
